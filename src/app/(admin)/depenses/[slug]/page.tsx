import{redirect}from"next/navigation";import{getWorkspaceHeader,listExpenses}from"@/actions/workspace.actions";import ExpensesClient from"./ExpensesClient";export default async function Page({params,searchParams}:{params:Promise<{slug:string}>;searchParams:Promise<{page?:string}>}){const{slug}=await params;const sp=await searchParams;const[h,d]=await Promise.all([getWorkspaceHeader(slug),listExpenses(slug,Number(sp.page)||1)]);if(!h.success)redirect("/");return <ExpensesClient slug={slug} header={h.data} initialData={d.success?d.data:null}/>}
